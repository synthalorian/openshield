[package]
name = "openshark"
version = "1.1.0"
edition = "2024"
authors = ["synth <synthalorian@gmail.com>"]
description = "The harness that learns. The agent that decides."
license = "MIT"
repository = "https://github.com/synthalorian/openshark"
keywords = ["ai", "harness", "coding", "agent", "llm"]
categories = ["command-line-utilities", "development-tools"]
rust-version = "1.91"

[[bin]]
name = "openshark"
path = "src/main.rs"

# Library target: same modules as the bin, exposed for embedding
# (Tauri Android backend links this to host `openshark serve` in-process).
[lib]
name = "openshark"
path = "src/lib.rs"

[features]
default = ["discord", "telegram", "slack", "matrix", "web-api"]
discord = ["serenity"]
telegram = ["teloxide"]
slack = ["slack-morphism"]
matrix = ["matrix-sdk"]
hermes-bridge = []
claw-delegation = []
opencode-delegation = []
claude-delegation = []
github-native = []
web-api = ["axum", "tokio-util"]

[dependencies]
# Async runtime
tokio = { version = "1.44", features = ["full"] }

# CLI + TUI
clap = { version = "4.5", features = ["derive"] }

# HTTP + API
reqwest = { version = "0.12", default-features = false, features = ["json", "stream", "blocking", "rustls-tls", "http2", "charset", "system-proxy"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
eventsource-stream = "0.2"
futures = "0.3"
urlencoding = "2.1"

# Config + Utils
toml = "0.8"
serde_yaml = "0.9"
dirs = "6.0"
anyhow = "1.0"
thiserror = "2.0"
chrono = { version = "0.4", features = ["serde"] }
uuid = { version = "1.16", features = ["v4", "serde"] }
unicode-width = "0.2"
walkdir = "2.5"
regex = "1.11"
base64 = "0.22"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
shellexpand = "3.1.2"
async-stream = "0.3"
tower = "0.5"
serenity = { version = "0.12", features = ["client", "gateway", "model", "cache", "rustls_backend"], optional = true }
async-trait = "0.1"
teloxide = { version = "0.17", features = ["macros"], optional = true }
slack-morphism = { version = "2.22", optional = true }
matrix-sdk = { version = "0.17", optional = true }
# Optional integration deps (only compiled when feature enabled)
octocrab = { version = "0.43", optional = true }
axum = { version = "0.8", features = ["ws"], optional = true }
tokio-util = { version = "0.7", features = ["compat"], optional = true }
rusqlite = { version = "0.37", features = ["bundled", "chrono"] }
tungstenite = "0.29.0"

# Desktop-only deps (clipboard + terminal UI) — not buildable/meaningful on Android.
[target.'cfg(not(target_os = "android"))'.dependencies]
arboard = { version = "3.5", default-features = false, features = ["wayland-data-control", "image-data"] }
crossterm = "0.29"

[dev-dependencies]
tokio-test = "0.4"

[profile.release]
opt-level = 3
lto = true
strip = true
